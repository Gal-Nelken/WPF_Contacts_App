<UserControl x:Class="Contacts_App.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Contacts_App.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="600">
    <Grid Margin="0,0,0,10"  Background="white">

        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="*" SharedSizeGroup="Name"/>
            <ColumnDefinition Width="*" SharedSizeGroup="Email"/>
            <ColumnDefinition Width="*" SharedSizeGroup="Phone"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,10,0" Grid.Column="0"  >
            <TextBlock x:Name="firstNameTextBlock" Text="First Name" Margin="0,0,5,0"/>
            <TextBlock x:Name="lastNameTextBlock" Text="LastName" />
        </StackPanel>
        <TextBlock x:Name="emailTextBlock" Text="Emailasda@gagd.com" Margin="0,0,10,0" Grid.Column="1" />
        <TextBlock x:Name="phoneTextBlock" Text="050-5658745" Grid.Column="2" />
    </Grid>
</UserControl>
